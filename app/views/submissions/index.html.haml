.container
  -# .submission#submission-show{data: {submission_id: @submissions.pluck(:id)}}
  .submission_head
    .create_link
      = link_to new_submission_path do
        = image_tag 'UI Bars/Status Bars/photo-camera (1).png'
  - @submissions.each do |submission|
    = render 'commons/submission', submission: submission